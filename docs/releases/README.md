# Releases

The release script writes versioned release notes here as `vX.Y.Z.md`.

Do not edit these by hand unless correcting release notes before the tag is pushed. Draft day-to-day notes in `docs/devlogs/` first.
